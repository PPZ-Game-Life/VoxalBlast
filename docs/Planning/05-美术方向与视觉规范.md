# VoxalBlast 美术方向与视觉规范

> **v0.8.23 · 木质田园「木作工坊」** / 2026-09-22
> 本版是**已实现**的现行美术规范，依据 `src/rendering/config.js`、`woodTexture.js`、`pastoralBackdrop.js`、`toyLights.js`、`blockResources.js`、`boardView.js`、`src/main.js`、`src/toy.css`、`src/game/shapes.js`。
> 现行修订：**98 个唯一方块组成 5×5×5 外壳，六面 150 个表面格共享棱角实体**；方块采用 AI 绘制的中性色素底图、三组裁切旋转变体、低起伏高度图与粗糙度图；矩形 HDR 光箱形成清漆亮边，保留暖色接触 AO 和统一 ACES。沿用 v0.8.17 候选色与现行低俯角相机。
> 材质目标为用户于 2026-09-17 再次指定的参考图：暖蜜桃木色、低对比手绘云纹、平整块面、柔亮倒角与暖褐接缝。本版记录实现与参数，不代表已达到参考图或完成新一轮验收。
> v0.7.1–v0.7.4 的形态演进与当时验收保留在 §2.1 / §8，不代表当前参数。
> v0.5.0「冷色塑料玩具」快照见 [归档](../Archive/Planning/05-美术方向与视觉规范-v0.5.0.md)，v0.6.0「六面表面表达重构」提案见 [归档](../Archive/Planning/05-美术方向与视觉规范-v0.6.0.md)。

---

## 0. 方向结论（一句话 + 四条硬规则）

**一座摆在田园草坡上的木质玩具：木头是物体，彩色是唯一的高饱和。**

1. **木材 = 物体**。整块棋盘是 98 个唯一方块；空格为原木，占用格为覆盖木纤维的彩漆。UI 使用暖木边与奶油纸芯，以倒角、光照和下沿厚度表现体积，不靠密集木纹。
2. **彩色 = 内容**。画面上只有「已放置方块」和「落点/道具预览」允许高饱和；其余一律落在木色—奶油纸色区间。
3. **背景 = 布景**。田园风景永远在棋盘之后、永远是柔和低对比的，且**不接收任何指针事件**。
4. **一个方块只有一个尺寸**。`0.94` 边长，块面平整、棱边倒角 `0.085`；邻块间隙 `0.06`。放置不改变块尺寸、高度或位置，棋盘、候选和拖拽沿用同一几何。

---

## 1. 资产边界（v0.8.0 修订）

本轮用户授权为接近参考效果选择合适方案，包括 AI 美术资源，替代旧版「运行时零外部素材」与「生成图只能作为草稿」限制。**交互积木继续使用实时 3D，静态田园布景使用随项目发布的 AI 绘制 WebP**；UI、图标、光照和特效仍由代码实现。运行时无需调用生图服务。

现行资产分工：

| 资产 | 实现 | 文件 |
| --- | --- | --- |
| 原木与彩漆表面 | AI 中性笔触底图 768×768 WebP（26,764 bytes），裁切旋转为三组 256×256 表面；分离色彩、高度与粗糙度；加载前/失败时保留程序纹理 | `public/art/block-pigment.webp`、`src/rendering/woodTexture.js` |
| 环境反射 | 256×128 线性 HDR HalfFloat 程序纹理，由各 renderer 独立 PMREM 过滤 | `src/rendering/toyLights.js` |
| 田园背景 | AI 绘制 1672×941 WebP，324,012 bytes；居中 `object-fit: cover` | `public/art/pastoral-valley.webp` |
| 背景兜底 | 启动时拼出的内联 SVG；加载前或 WebP 失败时保持可见，viewBox 1440×1000 | `src/rendering/pastoralBackdrop.js` |

程序纹理、UI 木纹和 SVG 花丛保持确定性，不随刷新改变；插画是版本固定的文件。来源、参考图使用方式、提示词与转换说明见 [背景资产说明](../../public/art/README.md)。

---

## 2. 棋盘 = 98 个唯一方块组成的外壳

**构成模型**：5×5×5 晶格外壳有 `5³ − 3³ = 98` 个唯一格；六面各 25 格合计 150 个表面格，其中棱/角指向同一实体。`buildFaceTiles()` 按 cell 坐标去重创建 98 个 mesh，每个 mesh 的 `userData.faces` 保存其所属面；Three.js 场景树只保留一个父 group。不能为每个面重复创建共面的棱角块，否则会产生重叠、闪烁与不一致的受光。

每块边长 `0.94`、倒角 `0.085`，主体面保持平整，中心在晶格格心，相邻块留 `0.06` 缝隙。最外块面在中心两侧 `±2.47`，读作等大积木拼起的整体。

壳体（`cubeBody`）是背板，用于遮挡穿透视线并提供深色缝底。`hullInset = 0.68` 是**总边长减少量**：背板边长 `5 − 0.68 = 4.32`，相对名义五格外表面单侧缩进 `0.34`，相对实际块面回退 `2.47 − 2.16 = 0.31`，不是单侧缩进 0.68。

统一几何：`blockGeometry = RoundedBoxGeometry(blockSize, blockSize, blockSize, blockSegments, blockRadius)`，**同一个 geometry 实例**给棋盘 98 块、三个候选槽、拖拽幽灵共用，主页缩影也复用场景几何。

| 参数 | 值 | 入口 |
| --- | --- | --- |
| 方块边长 `blockSize` | 0.94（间隙 = 1 − 0.94 = 0.06） | `BOARD_STYLE` |
| 棱边倒角 `blockRadius` / 分段 `blockSegments` | 0.085 / 3（平面宽度约占 82%） | `BOARD_STYLE` |
| 方块位置 | 晶格格心，无任何轴向偏移 | `buildFaceTiles` |
| 背板 `hullInset` | 0.68（总尺寸减少量） | `BOARD_STYLE` |
| 背板尺寸 / 圆角 | 5 − 0.68 = 4.32 / 0.08 | `main.js` |
| 落点预览 `previewLift` | 方块外表面再外移 0.03 | `BOARD_STYLE` |

**放置一个方块 = 把那一格的材质从「原木」换成「漆」。** 不新增网格、不改尺寸、不改位置、不改朝向、不改高度。棱格/角格本来就是一个 mesh，自然形成相邻面同色的包角；当前面提亮检查 `faces.includes(front)`，不会因该 mesh 挂在哪个 group 而漏掉共享格。

> 背板保留小圆角 `0.08`；几何调整后应检查八角和斜视缝隙，不用明亮背板伪装缝底。

### 2.1 演进记录

| 版本 | 模型 | 为什么错 |
| --- | --- | --- |
| v0.5 / v0.7.0 | 已放置方块 = 独立凸出 0.30 的 chip | 棱/角格必须挑一个面出手 → 正面下沿的角格同时在底面长出一片，方块从立方体底部挂出来 |
| v0.7.1 | 面片贴壳、几乎齐平（厚 0.10） | 几何把圆角钳到 0.05，读作"平贴纸片"；且壳体比面片浅，缝隙是浅色勾缝 |
| v0.7.2 | 面片整体抬高 0.13、加厚 0.30 | 变成了绗缝软垫：整个立方体像垫子，不是"平的方块拼起来" |
| v0.7.3–v0.7.4 | 150 个按面创建的 0.91³ 立方体 + 0.09 缝隙 | 棱角同一格重复创建；真实外壳只有 98 个唯一实体 |
| **v0.8.0–v0.8.5（现行模型）** | **98 个唯一 0.94³ 立方体 + 0.06 缝隙** | 按晶格去重；v0.8.5 增加云纹、粗糙度变化与接触 AO，倒角调为 0.085 |

关键教训：**`RoundedBoxGeometry` 的圆角半径会被钳到"最短边的一半"以内**。用薄片当方块时圆角必然被吃掉，所以"方块"必须是真正的立方体，而不是薄片加抬升。

---

## 3. 色彩基线

| 用途 | 值 | 说明 |
| --- | --- | --- |
| 页面天空底（`html` 背景） | `#8FD0F2 → #C3E7FB → #D8F0DC` | 背景 SVG 之前的兜底，也是负 z-index 层可见的前提（见 §5 注） |
| 背板 hullColor | `#765033` | 深木色缝底，位于 0.06 的块间缝隙之后 |
| 方块本色 blockColor | `#FF9C66` | 在 ACES 下校准为暖蜜桃木色；逐块按 `blockToneSteps` = `[0.94, 0.97, 1, 1.02, 1.04, 1.06]` 取色调 |
| 方块（当前操作面） | `#FFA16C` | 沿用同样的六档取色，只作「这一面可操作」的轻提示 |
| 方块漆色（单屏 ≤5 类） | `#C22B58`/`#3F8FE0`/`#293894`/`#8B57C9`/`#217D6E`/`#2F5FC4`/`#E0658F`/`#4FAA4A`/`#C03FA0`/`#35B6C9`/`#3FA87A`/`#90C22D`/`#6A5FB0`/`#A94FC4` | 蜡笔漆，饱和度比 v0.5 降一档，色相拉开；**只覆盖材质，不改变尺寸与位置**。v0.8.12~v0.8.14 新增的四色依次填色相环上原有的空档：`#3FA87A` 春绿、`#90C22D` 黄绿、`#6A5FB0` 蓝紫（同时靠明度与两种蓝分开）、`#A94FC4` 兰紫（补紫 269°→品红 318° 之间最大的一处空档）。**v0.8.17 起漆色不得落在木色带内**，见下表 |
| **木色带禁令（v0.8.17）** | 禁止区：色相 **0°–95°**（棋盘木材渲染实测色相 ≈27°） | 生产反馈「有几个备选方块和空置的木色方块太接近、看不清」。放置只换材质不换几何，所以"这格有没有块"完全由颜色承担——落在木色带里的漆色等于把这个答案答错。四个越界色改判：`Dot` `#E8543F`(h7°,ΔE 40.6)→`#C22B58` 树莓(56.0)、`Line 3` `#F2B52B`(h42°,36.8)→`#293894` 深海军蓝(90.5)、`L` `#EF9127`(h32°,30.5)→`#217D6E` 深青(52.7)、`L 5` `#93B23C`(h76°,44.2)→`#90C22D` 黄绿(57.4)。判定标准：任一漆色对木材三档渲染色（正面 `#D3A36F`／本色 `#E4A16D`／顶面 `#E3C7AC`）必须**色相≥35° 或 \|ΔL*\|≥22**。**`Line 3` 用深蓝而非让 `L` 用蓝**：`L`/`J` 互为镜像形状且 `J` 已是蓝色，镜像件同色会读成同一块。附带修掉一处旧冲突：`Dot` 原色与无效落点色 `#E8543F` 逐位相同（拖 `Dot` 到非法格时"放不下"提示与拼块同色，等于不提示），树莓色与之相距 ΔE 33.6 |
| 落点预览 | 有效＝**手上拼块自身的漆色**（半透明 0.72） / 无效 `#E8543F` | 该格方块的半透明幽灵 + 同色系深描边（有效时描边取漆色 ×0.58）。v0.8.11 前有效态固定用 `#7ED957`，与候选颜色无关——玩家看到的是"一个绿块"而不是"正在放的那个块"。v0.8.17 起 `#E8543F` 不再是任何拼块的漆色，无效态因此永远与手上拼块不同色 |
| 文字 | 主 `#452E13`（树皮墨）/ 次 `#7D5C33` | 木牌与奶油纸上的唯一文字色系，不再用海军蓝 |
| UI 木牌 | `--wood-1..6`（`#ECCB98` → `#5B3C19`）+ `--cream-1..3` | 见 `src/toy.css` 的 `:root` |

方块明暗由共享灯光、环境反射与实时阴影形成，不使用固定的逐面明度乘数。柔亮边来自倒角和清漆反射，纹理中不烘焙光向，旋转时受光随几何变化。

**v0.8.19 旧局配色兼容（2026-09-22）**：棋盘存档保存的是 RGB，不是形状名，单改候选池会让继续游戏仍显示旧橙/黄色。`session.migrate()` 现在将已知历史颜色映射到对应形状的当前漆色：橙→深青、黄→深蓝、陶红→树莓、旧橄榄→黄绿，并兼容 v0.7 之前的十色表。只改已知颜色，保留坐标、分数、道具、姿态和未知颜色；无需开新局。新增旧存档 fixture 的桌面/手机恢复截图验收，避免只看新游戏漏掉此问题。

---

## 4. 材质与光照

`blockSurfaceMaps(painted, variant)` 为原木和彩漆各缓存三组 256×256 纹理。AI 底图去除平均亮度和自身色偏，仅保留笔触、色素与浅木纹；原木纹理强度 2.15，彩漆 1.5。色彩图 `map` 使用 sRGB；独立高度图 `bumpMap` 与 `roughnessMap` 保持数据色彩空间。粗糙度图同时用于 `clearcoatRoughnessMap`，边肩更光滑，中央保留笔触；大块色差不再转成明显凹凸，避免软垫感。纹理不烘焙方向高光，UV 边界收敛。WebP 异步成功后原位更新纹理并重绘按需主页，失败保留程序纹理，不阻塞游戏；背板和 UI 沿用独立入口。

`toneIndexFor()` 以晶格坐标确定原木六档色调与纹理变体，避免相邻块完全重复。棋盘、候选、拖拽沿用同一套几何与材质生成入口。

| 材质参数 | 原木方块 | 彩漆方块 | 背板 |
| --- | --- | --- | --- |
| 材质类型 | `MeshPhysicalMaterial` | `MeshPhysicalMaterial` | `MeshPhysicalMaterial` |
| `roughness` | 0.43 | 0.3 | 0.7 |
| `clearcoat` / `clearcoatRoughness` | 0.65 / 0.18 | 1 / 0.12 | 0.1 / 0.42 |
| `bumpScale` | 0.009 | 0.005 | 无凹凸图 |

`roughnessMap` 调制基础粗糙度，使块面反光有轻微变化；不把整面做成镜面。`toyLights.js` 的半球光为天空 `#FFF4DF`、地面 `#A49A7B`、强度 0.65；主光 `#FFEFD6`、强度 2.4、位置 `[-3.5, 7, 5]`；补光 `#C9E3FF`、强度 0.5、位置 `[5, 2, -4]`；轮廓光 `#FFE6C4`、强度 0.75、位置 `[-4, 4, -5]`。

共享环境为 256×128 线性 HDR HalfFloat 纹理，各 renderer 独立 PMREM 过滤；`environmentIntensity = 0.7`。反射主光箱改为软矩形，强度 14、切平面宽/高参数 0.48 / 0.16；轮廓光箱强度/集中指数为 `4 / 32`，方向分别跟随主光与轮廓光。主画布在 HalfFloat composer 的末端执行一次 ACES；候选与主页由 renderer 执行 ACES，曝光统一为 1。

主光阴影使用 PCFSoftShadowMap，低性能档 1024、高档 2048。主棋盘另有暖褐 `#60422E` 接触 SSAO：`radius 0.075 / intensity 1.65 / bias 0.012 / fade 0.018`，世界接近阈值/衰减 `0.35 / 0.45`，亮度影响 0.15。桌面采样/圈数/分辨率比例为 `16 / 3 / 0.75`，移动或低性能档为 `11 / 3 / 0.5`。

AO 使用独立 `NormalPass` 生成的法线和真实深度，避免 composer 深度附件自拷贝；具体链路与 resize 顺序见 [技术结构](../Technical/ARCHITECTURE.md)。禁用玻璃、霓虹、随机高光、逐块不同光向及过量辉光。

---

## 5. 背景（田园布景）

`installPastoralBackdrop(#app)` 在启动时把 `.pastoral-backdrop` 插为 `#app` 的第一个子元素。

> ⚠️ **绘制顺序陷阱（本版踩过）**：`z-index:-1` 的层画在根层叠上下文的第 2 步，而 **`body` 自己的背景画在第 3 步——在它之上**。所以天空渐变必须挂在 `html`（传播到画布，最底层），`body` 必须是 `transparent`，否则整张田园风景会被一层纯色渐变静默盖住。`src/toy.css` 末尾的「Backdrop paint order」段即为此。

- 层序（后→前）：天空渐变 → 太阳辉光 → 云 → 远山 → 中景丘陵 → 村庄（含教堂尖顶） → 河流与石桥 → 树线 → 草坡 → 木栅栏 → 左上角大树 → 草簇与雏菊 → 地平线雾气 + 暗角。
- 取景：`viewBox 1440×1000` + `xMidYMid slice`。桌面裁上下、手机取中间约 35% 宽度——**因此草地、丘陵、河流必须横向通铺**，村庄居中而非塞在角落。
- 交互：`pointer-events: none` + `aria-hidden`，永不接收事件、不可聚焦。
- 对比度约束：布景整体低对比，画面最亮最饱和的永远是立方体。

---

## 6. UI / HUD（`src/toy.css`，v0.8.20 按钮与主页修订）

一条材质规则：**玩家能点的是木头（木纹 + 下沿加深 = 厚度），游戏显示在木头上的内容是奶油纸内嵌。**

| 控件 | 规格 |
| --- | --- |
| SCORE / BEST / CHAIN 牌 | 木纹 + `linear-gradient(180deg, --wood-1, --wood-2, --wood-3)`，`border-bottom: 5px solid --wood-5`，树皮墨文字 |
| 按钮（设置/操作说明/道具） | 深木侧壁、暖橙倒角框、内缩 4–5px 的奶油木面；木面复用低对比笔触 WebP，柔亮上沿与下沿投影表现厚度；SVG 图标使用棕色轮廓和木雕明暗填色 |
| 道具徽标 | 按参考图改珊瑚红至深红渐变、奶油描边、白色数字；仅用于剩余数量 |
| 候选托盘 `.bottom-panel` | 木框：`border-bottom 7px solid --wood-5` + 内阴影，圆角 26 |
| 候选卡 `.piece-slot` | 奶油纸内嵌（`--cream-1..3`）+ 内阴影 + `border-bottom 5px solid --wood-4` |
| 状态提示 / toast | 奶油纸标签 + 木边 |
| 弹窗 / 设置行 | 奶油纸面板 + 木纹底 + `border-bottom 8px solid --wood-4` |
| 主页 CTA | 蜜蜡黄木框立体钮，次级入口共用木框；主页保留半透明草坡遮罩，但 `.home-open` 下局内 HUD 和游戏层 `visibility:hidden` 且 `inert`，不会透出或误点；主页独立展示方块保留 |

候选图按真实三维包围盒变换到相机空间后适配正交视野，两轴均保留留白，避免 3×3 九宫格倾斜投影超过原固定视野；小形状保持最低视野，不随形状缩小而无限放大。候选卡仍裁切溢出内容，但正常拼块应全部落在卡内。
| 焦点环 | `#FFB62E` |

主次原则不变：**棋盘 > 当前分数 > 候选块 > 道具/设置/版本/装饰。**

`src/toy.css` 的 v0.7 段**只覆盖颜色/边框/阴影/材质**，绝不重声明 `height/width/gap/padding/order`——那些归 `styles.css` 与 v0.5 段的媒体查询，重声明会静默改掉手机布局。新增规则必须追加在**所有 v0.5 媒体查询之后**（同优先级下后写的生效）。

动效边界沿用 v0.5（`prefers-reduced-motion` 行为不变）；`feedbackSurfaceOffset` = `0.62`（方块外表面在 0.455，效果起点落在方块之外一点）。

**开场两阶段动画（03 §6，v0.8.22）**：进入一局时 98 个表面格块由一道斜向（屏幕左下 → 右上、跨面，不是逐面播放）波次**先造出来、再上色**。第一阶段每一块都穿**底漆**：同一族三档冷灰蓝 `#7d8b9c / #93a1b2 / #a9b6c5`，按格块在立方体上的高度分档，粗糙度/清漆统一成一"遍漆"，**不得出现任何木色或候选池漆色**；停 0.22s 后第二阶段用同一道波次把每块刷成它自己的正式色（`color` 由底漆线性插值到该材质原本的颜色，`roughness/clearcoat/bumpScale` 同步回位，并有一次 1.05 的"盖印"脉动）。单块 0.30s（造）+ 0.26s（刷），波带 0.042s/0.038s，整段约 2.83s——比 v0.8.21 的单阶段 1.05s 明显拉长，是"太快、看不清"反馈后的定稿。彩色已占用格块晚 0.05s 上色，并是**唯一**获得 `0.22` 轻微亮度提升（`emissive`，低于 bloom 阈值 1.35，不辉光）的格块。约束：不做扫描线、粒子堆叠、镜头震动或强闪光；未建格块只是尚未出现（不显示线框或占位），露出的是背后的深色外壳，与"缝隙即外壳色"的玩具木作规则一致。结束必须与动画前完全一致——位姿、缩放、颜色、共享材质与阴影全部显式还原。参数全部在 `src/rendering/config.js#INTRO_STYLE`；`prefers-reduced-motion` 时为 0.18s 整盘淡入直接落到正式色，不播底漆。

---

## 7. 反 AI 制作约束

1. 先写清用途/尺寸/状态/明暗方向，再谈表现。
2. 用可复用基础几何 + 明确比例，同类资产只做少量有意变体。
3. 光源、透视、圆角半径、线宽、阴影软硬度全项目统一；发现多套光照优先返工。
4. 禁随机细节填充：随机划痕、随机高光/装饰/渐变、无意义字母、乱码。
5. 生成式素材只能当草稿，须重建为可编辑的矢量/CSS/Shader/低复杂度模型并人工检查。
6. UI 文案、图标、数值一律用明确设计稿。
7. 新资产过「缩小到实际游戏尺寸」和「灰度模式」两道检查。
8. **一切「体积」必须能写成一个数**（见 §0.4）。

---

## 8. 历史验收清单（v0.7.1–v0.7.4）

以下保留当时的尺寸、参数与检查结果，不作为 v0.8.5 的新验收结论；现行参数见 §2–§4。

- [x] 缩到 240px 宽：第一眼是「明亮的彩色积木消除游戏」，不是深色疙瘩立方体。
- [x] **六面 150 个方块尺寸完全一致（0.91³），任何一格都不比邻格高、厚、大。**
- [x] **每个方块的六面是平的**（只有 0.05 棱边倒角），不是圆鼓的垫子块。
- [x] 方块外表面构成大六面体的表面；邻块之间有 0.09 的深色缝。
- [x] **放置只改变颜色：方块的几何、位置、朝向在整个单局内不变。**
- [x] 背板只在缝里露出来，颜色比方块深；8 个角上不透光。
- [x] 每个原木方块有自己的色调（±6% 六档），整体读作"一块块拼起来的木头"而非一体成型。
- [x] 背板 / 原木方块 / 漆色方块在灰度下三级可区分。
- [x] 棱格/角格在相邻面上同色相连，形成干净的包角；没有任何一块从立方体挂出来。
- [x] 落点预览是「该格方块本身的半透明幽灵」，与邻块同样留缝、不遮挡。
- [x] 候选 / 拖拽 / 棋盘三处是**同一个 geometry 实例**、同一明暗档、同一木纹与同一漆面参数。
- [x] 木纹在 3D 与 UI 上同源（棋盘与木牌是同一材料）。
- [x] `body` 透明、天空渐变在 `html`：田园背景确实可见（DOM 探针 + 截图双证）。
- [x] 桌面 1440×900 与手机 500×1082（headless 布局视口下限）各一张实机截图。
- [x] `npm test` 216/216；`npm run build` 通过；玩法 / 计分 / 存档无回归。

---

## 9. 相关文件

| 关注点 | 文件 |
| --- | --- |
| 色板、棋盘尺寸与厚度、光照参数、VFX 常量 | `src/rendering/config.js`（`RENDER_PALETTE` / `BOARD_STYLE`） |
| 木纹生成（3D CanvasTexture + UI data URL） | `src/rendering/woodTexture.js` |
| 田园背景 SVG | `src/rendering/pastoralBackdrop.js` |
| 灯光装置 | `src/rendering/toyLights.js` |
| 方块几何、方块着色、落点预览、道具覆盖 | `src/rendering/blockResources.js`（`blockGeometry` / `toneIndexFor` / `paintMaterial`）、`src/rendering/boardView.js`（`buildFaceTiles` / `applyTileMaterials`）、`src/rendering/pieceView.js`（落点预览与道具覆盖）、`src/main.js`（`renderBoard`） |
| 十四个方块的漆色（含 v0.8.17 木色带禁令） | `src/game/shapes.js` |
| UI 木牌与奶油纸内嵌 | `src/toy.css`（v0.7 段，文件末尾） |
